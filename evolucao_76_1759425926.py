# Legado de Prometheus (Geração 1)\n# Meta: Melhorar o uso de iteradores e list comprehensions para reduzir overhead de memória e melhorar a performance em operações que envolvem coleções de dados em Python.\n\n```
processadores = ['print("código antigo")']
for processor in processadores:
    globals()[processor]()
```